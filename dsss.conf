[burato/main_cli.d]
requires = gtkd
type = binary
target = ssh-tunnels-cli
buildflags = -L-ldl -no-export-dynamic

[burato/main_gtk.d]
requires = gtkd
type = binary
target = ssh-tunnels-gtk
buildflags = -L-ldl -no-export-dynamic

[unittests]
type = library
buildflags=-I../
